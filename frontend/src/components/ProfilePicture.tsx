import styled from 'styled-components';
import styledComponentWithProps from "src/style/styledComponentWithProps";

const ProfilePicture = styledComponentWithProps(styled.img`
    border-radius: 50%;
    width: 50px;
`);

export default ProfilePicture;
